# ============================================================
# 白泽 (Baize) — Python 依赖
#
# 安装:
#   pip install -r requirements.txt
#
# 或使用 uv (更快):
#   uv pip install -r requirements.txt
# ============================================================

# --- 核心数据源 ---
akshare==1.18.64           # A 股数据聚合 (东方财富/新浪/同花顺等)
mootdx>=1.0.0              # 通达信标准行情 (可选 — 更稳定的行情源)
baostock==0.9.2             # 证券宝 (免费历史数据)
requests==2.34.2            # HTTP 请求

# --- 数据处理 ---
pandas==3.0.3               # 数据分析核心
numpy==2.4.6                # 数值计算
pydantic==2.13.4            # 数据校验 / DTO

# --- 回测引擎 ---
backtrader==1.9.78.123      # 事件驱动回测框架

# --- 配置与格式 ---
PyYAML==6.0.3               # YAML 配置解析
python-dotenv>=1.0.0        # .env 文件加载

# --- CLI 美化 ---
rich==15.0.0                # 终端富文本输出
tabulate==0.10.0            # 表格格式化
tqdm==4.68.3                # 进度条

# --- 数据处理辅助 ---
openpyxl==3.1.5             # Excel 读写
xlrd==2.0.2                 # 旧版 Excel 读取
lxml==6.1.1                 # XML/HTML 解析
beautifulsoup4==4.15.0      # HTML 解析

# --- HTTP 辅助 ---
curl_cffi==0.15.0           # TLS 指纹模拟 (反爬)
mini-racer==0.14.1          # JS 引擎 (某些数据源需要)
